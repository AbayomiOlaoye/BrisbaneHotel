/* eslint-disable arrow-body-style */
import React from 'react';

const Button = () => {
  return (
    <button
      type="button"
    >
      Book a Room
    </button>
  );
};

export default Button;
